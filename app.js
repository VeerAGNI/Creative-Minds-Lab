document.addEventListener("DOMContentLoaded", () => { const themeToggle = document.getElementById("themeToggle"); const themeModal = document.getElementById("themeModal"); const selectLight = document.getElementById("selectLight"); const selectDark = document.getElementById("selectDark"); const htmlElement = document.documentElement; const savedTheme = localStorage.getItem("theme_mode"); if (!savedTheme) { themeModal.classList.remove("hidden"); } else { applyTheme(savedTheme); } function applyTheme(theme) { htmlElement.setAttribute("data-theme", theme); localStorage.setItem("theme_mode", theme); } selectLight.addEventListener("click", () => { applyTheme("light"); themeModal.classList.add("hidden"); }); selectDark.addEventListener("click", () => { applyTheme("dark"); themeModal.classList.add("hidden"); }); themeToggle.addEventListener("click", () => { const currentTheme = htmlElement.getAttribute("data-theme") || "light"; const newTheme = currentTheme === "light" ? "dark" : "light"; applyTheme(newTheme); }); });